package tp1.logic;

public class Commands {
	private String command = " ";
	
	public Commands(String command) {
		if (command.equalsIgnoreCase("update") || command.equalsIgnoreCase("U")) {
			command = "U";
		}
		
		else if (command.equalsIgnoreCase("UP") || command.equalsIgnoreCase("U")) {
			
		}
	}
}
